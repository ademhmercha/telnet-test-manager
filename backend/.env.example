# MongoDB
MONGO_URI=mongodb://localhost:27017/test-Telnet-Manager

# JWT — générez un secret fort pour la production (ex: openssl rand -hex 32)
JWT_SECRET=changez-ce-secret-en-production

# Serveur
PORT=3002
ALLOWED_ORIGIN=http://localhost:3000
