class Produit {
  constructor({ id, nom, posteId, description }) {
    this.id = id;
    this.nom = nom;
    this.posteId = posteId;
    this.description = description;
  }
}
module.exports = Produit;
